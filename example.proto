syntax = "proto3";

service Calculator {
    rpc CalculatePower(Operands) returns (Number) {}
}

message Number {
    double value = 1;
}

message Operands {
    Number num1 = 1;
    Number num2 = 2;
}